import { HEADER_LAYOUT, INTERFACE_MODE, LANGUAGE } from "@/constans/common";

export type InterfaceMode = `${INTERFACE_MODE}`;
export type Language = `${LANGUAGE}`;
export type HeaderLayout = `${HEADER_LAYOUT}`;
